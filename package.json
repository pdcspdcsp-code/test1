{
  "name": "scoreai",
  "version": "1.0.0",
  "description": "Sheet music image to digital score converter",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-flag=nodejs_compat",
    "deploy": "wrangler pages deploy public"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.52.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
